// This is the main function.
func gint main() {
     var gint x = 5; // This is a basic program.
     guif (x > 6) {
     	  guoturn guoaf(x);
     }
     // A stupid one at that.
     guoturn 0;
}

// Here is a function.
func gint guoaf(var gint x) {
     guoturn -2;
}