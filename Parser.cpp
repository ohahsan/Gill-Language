#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include "Parser.h"

using namespace std;

extern string word;
extern int number;

/* parse -

   Uses the lexer to generate tokens and then parses them. */

void parse(vector<TokenClass> tokens) {
  
}
