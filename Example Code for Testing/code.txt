// Please ignore this.
func gint main() {
     var gint x = 5;
     var gint y = x + x;
     guif (x > y) {
     	  var gint z = 0;
	  guoturn z;
     } elif (x < y) {
       	  return g(x);
     } guolse {
          // This is a comment.
          guoturn y;
     }
}

// Function g.

func gint g(var gint x) {
     return x*x; // x squared.
}
